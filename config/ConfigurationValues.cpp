#include "ConfigurationValues.h"

// Default constructor implementation (if needed)
ConfigurationValues::ConfigurationValues() noexcept: nEvents(0), eventType(Enumerations::EventType::FIXED_KEY_VALUE), hideOutput(false) {
    // Initialize or load default configurations here if needed
}
